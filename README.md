# NoBuyDays website

Static landing page for NoBuyDays, published with GitHub Pages.

## Local preview

```sh
python3 -m http.server 8080
```

Then open `http://localhost:8080`.

## Deployment

GitHub Pages serves the repository root from the `main` branch. Pushing to `main` updates the live website automatically.
